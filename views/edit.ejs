<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post - <%= post.title %></title>
    <style>
        body {
            font-family: 'Comic Sans MS', cursive, sans-serif;
            background: linear-gradient(135deg, #f8b400, #f768a2);
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        .container {
            max-width: 600px;
            background: #fff;
            padding: 30px;
            border-radius: 20px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
            text-align: center;
            position: relative;
            animation: float 5s ease-in-out infinite;
        }
        h1 {
            font-size: 2.5rem;
            margin-bottom: 20px;
            color: #333;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
        }
        form {
            display: flex;
            flex-direction: column;
        }
        input, textarea, button {
            margin-bottom: 15px;
            padding: 12px;
            border: 2px solid #ddd;
            border-radius: 8px;
            outline: none;
            font-size: 1.1rem;
            transition: all 0.3s ease;
        }
        input:focus, textarea:focus {
            border-color: #ff8c00;
            background: #fff5e6;
        }
        button {
            background-color: #ff5722;
            color: #fff;
            cursor: pointer;
            font-size: 1.2rem;
            font-weight: bold;
            border: none;
            transition: all 0.3s ease;
        }
        button:hover {
            background-color: #e64a19;
            transform: scale(1.05);
        }
        .back-link {
            display: block;
            margin-top: 20px;
            color: #ff5722;
            text-decoration: none;
            font-weight: bold;
            transition: color 0.3s ease;
        }
        .back-link:hover {
            color: #e64a19;
            text-decoration: underline;
        }
        /* Floating Animation */
        @keyframes float {
            0%, 100% {
                transform: translateY(-10px);
            }
            50% {
                transform: translateY(10px);
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Edit Post</h1>
        <form action="/posts/<%= post._id %>?_method=PUT" method="POST">
            <input type="text" name="title" value="<%= post.title %>" placeholder="Title" required>
            <textarea name="body" rows="10" placeholder="Body" required><%= post.body %></textarea>
            <button type="submit">Update Post</button>
        </form>
        <a href="/posts/<%= post._id %>" class="back-link">Back to Post</a>
    </div>
</body>
</html>
