<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create a Post - Makepost.com</title>
    <style>
        body {
            margin: 0;
            font-family: 'Comic Sans MS', cursive, sans-serif;
            background: radial-gradient(circle, #ffcfdf, #ff99c2);
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            overflow: hidden;
        }
        h1 {
            font-size: 3rem;
            color: #fff;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
            animation: bounce 2s infinite;
        }
        form {
            background: rgba(255, 255, 255, 0.9);
            border-radius: 25px;
            padding: 40px;
            box-shadow: 0 12px 24px rgba(0, 0, 0, 0.2);
            animation: slideIn 1.5s ease-in-out;
            width: 100%;
            max-width: 600px;
        }
        input, textarea, button {
            margin-bottom: 20px;
            padding: 15px;
            border: 2px solid #f8b400;
            border-radius: 8px;
            font-size: 1.1rem;
            transition: all 0.3s ease;
        }
        input:focus, textarea:focus {
            border-color: #ff7f50;
            background-color: #fff8f0;
        }
        button {
            background-color: #ff8c00;
            color: #fff;
            cursor: pointer;
            font-size: 1.2rem;
            border: none;
            transition: all 0.3s ease;
        }
        button:hover {
            background-color: #ff7f50;
            transform: scale(1.1);
        }
        /* Animations */
        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% {
                transform: translateY(0);
            }
            40% {
                transform: translateY(-30px);
            }
            60% {
                transform: translateY(-15px);
            }
        }
        @keyframes slideIn {
            0% {
                transform: translateY(-50px);
                opacity: 0;
            }
            100% {
                transform: translateY(0);
                opacity: 1;
            }
        }
    </style>
</head>
<body>
    <h1>Write a Post Here</h1>
    <form action="/posts" method="post">
        <input type="text" name="username" placeholder="Write your username" required>
        <input type="text" name="title" placeholder="Write the title of the post" required>
        <textarea name="body" id="body" rows="10" placeholder="Write the post here" required></textarea>
        <button type="submit">Create Post</button>
    </form>
</body>
</html>
